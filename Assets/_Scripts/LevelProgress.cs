using UnityEngine;
using UnityEditor;

namespace Assets._Scripts
{
    public class LevelProgress : MonoBehaviour
    {
        void Awake()
        {
            if (PlayerPrefs.HasKey("progress"))
            {

            }
        }
    }
}